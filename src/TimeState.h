#pragma once

// Better handled in renderer
struct TimeState {};

// Better handled in renderer
struct DateState {};

constexpr auto SOURCE_TIME_NAME = "Time";
constexpr auto SOURCE_DATE_NAME = "Date";
