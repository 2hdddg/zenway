#pragma once

#include <map>
#include <memory>
#include <string>
#include <vector>

/*
class WorkspaceApplication {
   public:
    WorkspaceApplication(){};

    std::string name;
    std::string app_id;
    bool focused;
    bool nextFocused;
};

struct Workspace {
    Workspace() : focused(false) {}

    std::string name;
    bool focused;
    std::vector<WorkspaceApplication> applications;
};
*/
